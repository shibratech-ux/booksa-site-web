import HomePage from '@/pages/home';

// Shared shell for the home page.
export function MainLayout() {
  return (
    <div className="project-shell min-h-screen text-[var(--color-text-primary)]">
      <HomePage />
    </div>
  );
}

  
